# Quantum Ooze Android

Native Android WebView packaging for the offline HTML5 game.

Build from Android Studio, or run `gradlew.bat assembleDebug` after the Gradle wrapper is installed.

The debug APK is written to `app/build/outputs/apk/debug/app-debug.apk`.
